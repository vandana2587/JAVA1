package Statement;

public class ternary {
    public static void main(String[] args) {
        int marks= 20;
        String result = (marks >= 18) ? "pass" : "fail";
        System.out.println(result);
    }

    
}
